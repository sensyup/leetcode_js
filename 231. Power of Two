/**
 * @param {number} n
 * @return {boolean}
 */
var isPowerOfTwo = function(n) {
    if(n<=0) return false;
    while(n%2==0){
        n=Math.floor(n/2);
    }
    return (n==1);
};

//位运算
var isPowerOfTwo = function(n) {
    if (n<=0) return false;
    return !((n-1)&n)
};
