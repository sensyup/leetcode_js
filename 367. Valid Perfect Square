/**
 * @param {number} num
 * @return {boolean}
 */
var isPerfectSquare = function(num) {
    if( num==1) return true;
    for(let i=1;i<=num/i;i++){
        if(i*i==num) return true;
    }
    return false;
};


1.这个题还可以用而二分查找法，不再赘述
2.完全平方数=一系列奇数之和，4=1+3,9=1+3+5...SO...
